spring:
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/iloveweilai/spring-cloud-config-demo.git
          #username: 
          #password:
          search-paths: SC-BOOK-CONFIG
  application:
    name: ch11-3-config-server-bus

#  rabbitmq:
#    host: localhost
#    port: 5672
#    username: guest
#    password: guest
server:
  port: 9090
